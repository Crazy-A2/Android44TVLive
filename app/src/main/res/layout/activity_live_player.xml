<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/background_black">

    <SurfaceView
        android:id="@+id/surface_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- 频道信息条（顶部） -->
    <FrameLayout
        android:id="@+id/channel_info_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:visibility="gone"
        android:background="@color/background_overlay"
        android:padding="@dimen/channel_info_bar_padding">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageView
                android:id="@+id/channel_logo"
                android:layout_width="@dimen/channel_logo_size"
                android:layout_height="@dimen/channel_logo_size"
                android:scaleType="fitCenter"
                android:visibility="gone" />

            <TextView
                android:id="@+id/channel_number"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/padding_small"
                android:textSize="@dimen/text_channel_number"
                android:textColor="@color/text_primary"
                android:gravity="center_vertical" />

            <TextView
                android:id="@+id/channel_name"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/padding_large"
                android:textSize="@dimen/text_channel_name"
                android:textColor="@color/text_primary"
                android:gravity="center_vertical" />

            <TextView
                android:id="@+id/channel_epg"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/padding_large"
                android:textSize="@dimen/text_epg"
                android:textColor="@color/text_epg"
                android:gravity="center_vertical"
                android:visibility="gone" />
        </LinearLayout>
    </FrameLayout>

    <!-- 音量条（底部） -->
    <FrameLayout
        android:id="@+id/volume_bar_container"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:visibility="gone" />

    <!-- 频道号输入（居中） -->
    <FrameLayout
        android:id="@+id/channel_number_input_container"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone" />

    <!-- 频道列表覆盖层（右侧） -->
    <FrameLayout
        android:id="@+id/channel_list_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- 设置覆盖层（居中） -->
    <FrameLayout
        android:id="@+id/settings_overlay"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone" />

    <!-- 加载/源不可用提示 -->
    <TextView
        android:id="@+id/status_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:textColor="@color/text_primary"
        android:textSize="@dimen/text_status"
        android:background="@color/background_light_overlay"
        android:padding="@dimen/padding_large"
        android:visibility="gone" />

</FrameLayout>
